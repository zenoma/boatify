defmodule StowawayTest do
  use ExUnit.Case
  doctest Stowaway

  # test "start_stoaway" do
  #   Stowaway.start(1)
  # end

  # test "levantar_servidor_stoaway" do
  #   {_,pid} = Stowaway.levantar_servidor(:Sandra)
  #   assert Process.alive?(pid) ==  true
  # end

  # test "parar_servidor" do
  #   {_,pid} = Stowaway.levantar_servidor(:Sandra)
  #   Stowaway.parar_servidor(:Sandra)
  #   assert Process.alive?(pid) ==  false
  # end

  # test "stop_stoaway" do
  #   Stowaway.start(1)
  #   Stowaway.stop
  # end
   
end


